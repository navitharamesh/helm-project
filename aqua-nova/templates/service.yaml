apiVersion: v1
kind: Service
metadata:
  name: {{ include "aqua-nova.fullname" . }}
  labels:
    {{- include "aqua-nova.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aqua-nova.selectorLabels" . | nindent 4 }}
