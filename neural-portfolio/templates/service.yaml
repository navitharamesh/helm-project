apiVersion: v1
kind: Service
metadata:
  name: {{ include "neural-portfolio.fullname" . }}
  labels:
    {{- include "neural-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neural-portfolio.selectorLabels" . | nindent 4 }}
